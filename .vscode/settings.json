{
    // VS Code workspace settings for astro_airflow_demo
    // ---------------------------------------------------
    // This workspace uses .venv_airflow as the default Python interpreter for VS Code.
    // This enables IntelliSense, linting, and debugging for Airflow DAG development.
    // dbt is kept in a separate venv to avoid dependency conflicts; activate .venv_dbt in the terminal for dbt CLI.
    //
    // To activate the Airflow environment in a new terminal:
    //   . .venv_airflow\Scripts\Activate.ps1
    // To activate the dbt environment in a new terminal:
    //   . .venv_dbt\Scripts\Activate.ps1
    //
    // The setting below ensures Python tools in VS Code use the Airflow environment by default for linting.
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv_airflow/Scripts/python.exe"
}
